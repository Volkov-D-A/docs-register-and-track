{
    "_comment": "LOCAL DEVELOPMENT EXAMPLE ONLY. Do not use these localhost endpoints, disabled TLS settings or example encrypted secrets as production defaults.",
    "server": {
        "url": "http://localhost:8080",
        "allowInsecureHttp": false
    }
}
